An attempt to recreate the Descriptive Stats page with Tailwind (previously done with Bulma).

Bundle js with:
npx webpack --mode development --watch

Then open dist/phenocode_semantic.html in a browser.
